const state = {
    coverUrl: ""
}

const mutations = {
    SET_COVER(state, url) {
        state.coverUrl = url
    }
}

const actions = {
    
}

export default {
    state,
    mutations,
    actions
}